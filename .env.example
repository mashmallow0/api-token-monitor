# Copy this file to .env and fill in your values

# Admin Token Hash (Argon2id hash of the admin token)
# Generate with: await argon2.hash('your-admin-token', { memoryCost: 65536, timeCost: 3 })
VITE_ADMIN_TOKEN_HASH=