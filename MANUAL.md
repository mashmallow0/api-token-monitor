# คู่มือการใช้งาน API Token Monitor

## 📋 สารบัญ
1. [เริ่มต้นใช้งาน](#เริ่มต้นใช้งาน)
2. [การเข้าสู่ระบบ](#การเข้าสู่ระบบ)
3. [หน้า Dashboard](#หน้า-dashboard)
4. [การจัดการ API Keys](#การจัดการ-api-keys)
5. [สำหรับ Admin](#สำหรับ-admin)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## เริ่มต้นใช้งาน

### ความต้องการของระบบ
- Node.js เวอร์ชัน 18 ขึ้นไป
- เบราว์เซอร์รองรับ Web Crypto API (Chrome, Firefox, Safari)

### ขั้นตอนการติดตั้ง

```bash
# 1. ดาวน์โหลดโปรเจค
git clone https://github.com/mashmallow0/api-token-monitor.git

# 2. เข้าไปในโฟลเดอร์
cd api-token-monitor

# 3. ติดตั้ง dependencies
npm install

# 4. ตั้งค่า environment
cp .env.example .env
# แก้ไขไฟล์ .env ใส่ admin token hash

# 5. เริ่มต้นใช้งาน
npm run dev
```

---

## การเข้าสู่ระบบ

### สำหรับผู้ใช้ทั่วไป
1. เปิดแอพในเบราว์เซอร์
2. กรอก **Token** ที่ได้รับจาก Admin
3. กดปุ่ม "เข้าสู่ระบบ"
4. ระบบจะพาไปยังหน้า Dashboard

### สำหรับ Admin
- ใช้ Admin Token: `?bpy&Z?@CNn&b%mJ`
- จะเห็นเมนู "Admin Panel" เพิ่มขึ้น

---

## หน้า Dashboard

### ส่วนประกอบหลัก
1. **วงกลมแสดง % การใช้งาน**
   - 🟢 เขียว: ใช้งานต่ำกว่า 50%
   - 🟡 เหลือง: ใช้งาน 50-80%
   - 🔴 แดง: ใช้งานมากกว่า 80%

2. **สถิติการใช้งาน**
   - จำนวน Request ที่ใช้ไป
   - จำนวนที่เหลือ
   - วันที่รีเซ็ตโควต้า

3. **รายการ API Providers**
   - แสดงแต่ละ provider พร้อม progress bar
   - คลิกเพื่อดูรายละเอียดเพิ่มเติม

4. **Activity Log**
   - ประวัติการใช้งานล่าสุด
   - แสดงสถานะ (สำเร็จ/ล้มเหลว)
   - เวลาที่ใช้ (latency)

---

## การจัดการ API Keys

### เพิ่ม API Key ใหม่
1. ไปที่หน้า **Settings**
2. กดปุ่ม "เพิ่ม Provider"
3. กรอกข้อมูล:
   - ชื่อ Provider (เช่น OpenAI, Anthropic)
   - API Key (จะถูกซ่อนเป็น ***)
4. กด "ทดสอบการเชื่อมต่อ"
5. ถ้าสำเร็จ กด "บันทึก"

### แก้ไข API Key
1. คลิกไอคอน ✏️ ที่ Provider ที่ต้องการ
2. แก้ไขข้อมูล
3. กด "บันทึก"

### ลบ API Key
1. คลิกไอคอน 🗑️ ที่ Provider ที่ต้องการ
2. ยืนยันการลบ

**⚠️ คำเตือน**: API Key ทั้งหมดถูกเข้ารหัส AES-256-GCM ไม่สามารถอ่านได้โดยตรง

---

## สำหรับ Admin

### สร้าง Token ใหม่
1. ไปที่หน้า **Admin Panel**
2. เลือก Role:
   - **Admin**: ควบคุมทุกอย่าง
   - **Manager**: จัดการ providers ได้
   - **User**: ดู dashboard อย่างเดียว
3. เลือก Permissions ที่ต้องการ
4. กด "สร้าง Token"
5. คัดลอก Token ที่แสดง (แสดงครั้งเดียว)

### จัดการผู้ใช้
- ดูรายการผู้ใช้ทั้งหมด
- ดู Role และ Status
- ลบผู้ใช้ที่ไม่ต้องการ

---

## ความปลอดภัย

### การเข้ารหัส
- **API Keys**: เข้ารหัส AES-256-GCM
- **Token**: แฮชด้วย PBKDF2-SHA256
- **การเปรียบเทียบ**: ใช้เวลาคงที่ (constant-time) ป้องกัน timing attack

### ข้อมูลจัดเก็บ
- ข้อมูลอยู่ใน browser (localStorage)
- ไม่มีข้อมูลส่งออกไป server
- ข้อมูลเข้ารหัสทั้งหมด

---

## การแก้ไขปัญหา

### ลืม Token
ติดต่อ Admin เพื่อขอ Token ใหม่

### เข้าระบบไม่ได้
1. ตรวจสอบว่า Token ถูกต้อง
2. รอสักครู่แล้วลองใหม่ (Rate Limit 5 ครั้ง/15 นาที)
3. ถ้ายังไม่ได้ ติดต่อ Admin

### API Key ใช้ไม่ได้
1. ไปที่ Settings
2. กด "ทดสอบการเชื่อมต่อ" ที่ Provider นั้น
3. ถ้าไม่สำเร็จ ตรวจสอบว่า API Key ถูกต้อง
4. อัพเดต API Key ใหม่ถ้าจำเป็น

### ข้อมูลหาย
- ข้อมูลจัดเก็บใน browser
- ถ้า clear browser data ข้อมูลจะหาย
- ให้สำรองข้อมูลสำคัญไว้

---

## ติดต่อสนับสนุน

หากมีปัญหา สามารถสร้าง Issue บน GitHub ได้

---

**จัดทำเมื่อ**: กุมภาพันธ์ 2026
**เวอร์ชัน**: 2.0.0
